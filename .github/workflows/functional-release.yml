name: functional-release

on:
  push:
    tags: [ "functional-v*" ]

jobs:
  release:
    name: Publish functional to npm
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    env:
      CI: true

    defaults:
      run:
        working-directory: functional

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: 'functional/.nvmrc'
          cache: 'npm'
          cache-dependency-path: 'functional/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Verify tag matches package version
        run: test "functional-v$(npm pkg get version | tr -d '\"')" = "$GITHUB_REF_NAME"

      - name: Run typecheck
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Publish
        run: npm publish
